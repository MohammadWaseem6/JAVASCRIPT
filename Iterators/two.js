let index = 0;
while (index <= 10) {
  // console.log(`value os index is ${index}`);
  index = index + 2;
}
let myarray = ["hello", "everyone", "today ", " is raining"];
let arr = 0;
while (arr < myarray.length) {
  // console.log(`value is ${myarray[arr]}`);
  arr = arr + 1;
}

let score = 0;

do {
    console.log(`score is ${score}`);
    score++;
} while (score < 20);
